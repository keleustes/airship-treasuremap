---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  labels:
    name: openstack-mariadb
  name: openstack-mariadb
  namespace: openstack
spec:
  values:
    pod:
      replicas:
        ingress: 1
        server: 1
